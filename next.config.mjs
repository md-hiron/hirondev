/** @type {import('next').NextConfig} */
const nextConfig = {
  //output: 'export',       // Enables static HTML export
  //trailingSlash: true,    // Optional: adds trailing slashes for better static hosting compatibility
};

export default nextConfig;
